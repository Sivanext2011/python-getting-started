apiVersion: v1
kind: Service
metadata:
  name: python-getting-started-prod-svc
spec:
  selector:
    app: python-getting-started
    env: prod
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
